# S3 credentials for immers S3 (s3.msk.immers.cloud).
# Real values come from ~/.config/immers/s3-ec2.env — copy this file to .env and fill them in.
# .env is git-ignored; do NOT commit real credentials.
MINIO_ACCESS_KEY=
MINIO_SECRET_KEY=
